// ============================================================
//  FIREBASE CONFIG  —  paste your project's keys below.
// ------------------------------------------------------------
//  Until you do, the app runs in DEMO MODE (data lives only on
//  this device and is NOT shared). Follow the steps in README.md
//  to create a free Firebase project, then replace the PASTE_...
//  values with the config it gives you.
//
//  These keys are safe to be public — Firebase is designed that
//  way. Your data is protected by the security rules (see README).
// ============================================================

export const firebaseConfig = {
  apiKey: "PASTE_YOUR_API_KEY",
  authDomain: "PASTE_YOUR_PROJECT.firebaseapp.com",
  projectId: "PASTE_YOUR_PROJECT_ID",
  storageBucket: "PASTE_YOUR_PROJECT.appspot.com",
  messagingSenderId: "PASTE_YOUR_SENDER_ID",
  appId: "PASTE_YOUR_APP_ID"
};

// Optional: set a simple family passcode (e.g. "1234") to put a
// gentle lock screen in front of the app. Leave "" for no lock.
export const APP_PASSCODE = "";
